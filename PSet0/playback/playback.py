"""
implement a program in Python that prompts the user for input 
and then outputs that same input, replacing each space with ... 
(i.e., three periods).
"""

say_sum = input("Say something: ").strip().replace(" ", "...")

print(say_sum)